import jwt from "jsonwebtoken";

class RefereshToken {

    constructor(){};

    public generateRefereshToken(email:string,password:string) {
        try {


        }catch(error) {
            console.log("===========Generating Referesh Token Error=====================");
        }
    }
}
var generatingRefereshToken = new RefereshToken;
export default generatingRefereshToken;